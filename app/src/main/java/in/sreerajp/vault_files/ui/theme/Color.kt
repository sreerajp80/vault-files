package `in`.sreerajp.vault_files.ui.theme

import androidx.compose.ui.graphics.Color

// Terracotta / Cream primary theme colors (warm)
val PrimaryPurple = Color(0xFFBF4A2E)        // terracotta accent (name kept for compatibility)
val LightPrimaryContainer = Color(0xFFF3DDD3)
val DarkPrimaryText = Color(0xFF4A1A0E)
val PolishBg = Color(0xFFF4ECDB)             // cream background
val PolishSurface = Color(0xFFFBF6EC)        // near-white warm tile
val PolishBorder = Color(0xFFC9A593)
val PolishTextBlack = Color(0xFF2C2620)
val PolishTextGrey = Color(0xFF6E5D52)

// Dark Theme variants (same warm family, inverted lightness)
val DarkPrimary = Color(0xFFE08763)          // lightened terracotta
val DarkBackground = Color(0xFF1B1714)
val DarkSurface = Color(0xFF2E2620)
val DarkOnSurface = Color(0xFFECE0D2)

// Clearly-identifiable tile / pill border colors (stronger than the faint design borders,
// so every tile and pill stays visible in both light and dark mode)
val TileBorderLight = Color(0xFFE0CDBD)
val TileBorderDark = Color(0xFF3E342C)
